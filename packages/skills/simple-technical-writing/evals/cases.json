{
  "version": 1,
  "cases": [
    {
      "name": "preserve-code-and-simplify",
      "input": {
        "intent": "rewrite",
        "text": "You should simply utilize `npm run build` prior to deploying, which will ensure the application has been compiled correctly."
      },
      "expect": {
        "contains": ["`npm run build`", "before"],
        "excludes": ["utilize", "simply", "should", "prior to"]
      }
    },
    {
      "name": "split-procedure",
      "input": {
        "intent": "rewrite",
        "text": "Check the backup and run the migration if it completed successfully."
      },
      "expect": {
        "semantic": [
          "verify the backup first",
          "run the migration only after success"
        ],
        "maxActionsPerInstruction": 1
      }
    },
    {
      "name": "preserve-identifiers",
      "input": {
        "intent": "rewrite",
        "text": "You can update source.connect_timeout_seconds in config.yaml."
      },
      "expect": {
        "exact": ["source.connect_timeout_seconds", "config.yaml"]
      }
    },
    {
      "name": "do-not-invent",
      "input": {
        "intent": "rewrite",
        "text": "The worker retries failed requests."
      },
      "expect": {
        "excludes": ["three times", "exponential backoff", "dead-letter queue"]
      }
    },
    {
      "name": "replace-uncommon-general-words",
      "input": {
        "intent": "rewrite",
        "text": "A transient settlement failure can overcharge a paid preview."
      },
      "expect": {
        "contains": ["temporary"],
        "excludes": ["transient"],
        "semantic": ["a temporary settlement error can charge too many credits"]
      }
    },
    {
      "name": "explain-required-technical-term",
      "input": {
        "intent": "rewrite",
        "text": "Malformed frozen exclusion snapshots fail open."
      },
      "expect": {
        "excludes": ["malformed"],
        "semantic": [
          "invalid saved exclusion data is accepted",
          "explain any required domain term in common words"
        ]
      }
    },
    {
      "name": "put-review-consequence-first",
      "input": {
        "intent": "rewrite review finding",
        "text": "The settlement claim falls back to the original reservation after an error. The customer loses the expected refund."
      },
      "expect": {
        "semantic": [
          "state the lost refund before the internal recovery behavior",
          "preserve the relationship between the error and the full charge"
        ]
      }
    },
    {
      "name": "apply-during-development-by-default",
      "input": {
        "intent": "complete a coding task",
        "text": "Fix the timeout bug and explain the result. I did not request a prose rewrite."
      },
      "expect": {
        "semantic": [
          "apply the writing rules to plans, updates, diagnoses, explanations, and the final summary",
          "do not apply the prose rules to source code or generated diffs",
          "do not require a separate request for clearer prose"
        ]
      }
    }
  ]
}
